[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[scripts]
    main = "pipenv run python pipenvExample/main.py"
    test = "pipenv run python -m unittest discover -s tests -p '*_test.py'"
    publish = "bash buildAndDeployPythonPackage.sh"

[dev-packages]
pipenv2setup = "*"
twine = "*"

[packages]
fnf = "<2.0.0"

[requires]
python_version = "3.4"
